const words = [
    "delay",
    "woozy",
    "rake",
    "pat",
    "abrasive",
    "like",
    "laugh",
    "collect",
    "trains",
    "productive",
    "teeny-tiny",
    "quaint",
    "luka"
  ];
//   const filteredArray = words.filter((word) => word.length < 5);
// const filteredArray = words.filter((word) => word.charAt(0) == "l");
//   const filteredArray = words.filter((word) => word.charAt(0) !== "r");
const filteredArray = words.filter((word) => word.charAt !== "-");
  console.log(filteredArray)